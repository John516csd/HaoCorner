# This project currently does not require environment variables for local development.
#
# Keep deployment secrets in .env.local or your hosting provider's environment
# variable settings. Do not commit private values to the repository.
